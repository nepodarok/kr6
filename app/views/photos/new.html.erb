<h1>Добавление фото </h1>
<%= form_for(@photo) do |f| %>

<%= render 'shared/error_messages', object: f.object %>

<%= f.label "Название фото" %>
<%= f.text_field :name %>

<%= f.label "Обложка" %><br />
<%= f.file_field :pic %>

<%= f.submit "Сохранить"%>

<% end %>
